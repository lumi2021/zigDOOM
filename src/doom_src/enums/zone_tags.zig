pub const ZoneTags = enum(i32) {
    static = 1,
    sound = 2,
    music = 3,
    david = 4, // anything else Dave wants static

    level = 50,
    levspec = 51,

    purgelevel = 100,
    cache = 101,
    _
};